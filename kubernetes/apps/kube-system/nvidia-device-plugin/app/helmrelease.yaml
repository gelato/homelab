---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/helm.toolkit.fluxcd.io/helmrelease_v2.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: &app nvidia-device-plugin
spec:
  interval: 30m
  chart:
    spec:
      chart: *app
      version: 0.18.0
      sourceRef:
        kind: HelmRepository
        name: nvidia-charts
  install:
    remediation:
      retries: 3
  upgrade:
    cleanupOnFail: true
    remediation:
      strategy: rollback
      retries: 3
  values:
    image:
      repository: ghcr.io/nvidia/k8s-device-plugin
      tag: 9762cfb5-amd64@sha256:d43897de274b63c534aafaeae651057b39b5561b7be6cdda01a564e6afd4d3f2
    runtimeClassName: nvidia
    securityContext:
      privileged: true
    config:
      map:
        default: |-
          version: v1
          flags:
            migStrategy: none
          sharing:
            timeSlicing:
              renameByDefault: false
              failRequestsGreaterThanOne: false
              resources:
                - name: nvidia.com/gpu
                  replicas: 8